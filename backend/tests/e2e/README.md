# Test end-to-end backend/frontend

Questo file funge da placeholder per indicare che i test end‑to‑end sono definiti nella root `tests/e2e` del progetto (vedi Playwright).

Non sono richieste modifiche specifiche al backend per il Task 1080, in quanto le API per la gestione collaboratori aziendali sono già implementate nelle story precedenti. I test e2e sfruttano tali API tramite la UI.
