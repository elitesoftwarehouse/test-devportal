// Comandi custom Cypress per Elite Portal
// (al momento non necessari per questi test specifici)

Cypress.Commands.add('loginAsStandardUser', () => {
  // Implementare qui il flusso di login reale se richiesto dai test futuri.
});
